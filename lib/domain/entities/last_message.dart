class LastMessage {
  final String text;
  final String authorId;
  final String createdAt;
  const LastMessage({
    required this.text,
    required this.authorId,
    required this.createdAt,
  });
}