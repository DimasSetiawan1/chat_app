import 'package:flutter/material.dart';

class CustomActionCard extends StatelessWidget {
  const CustomActionCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}